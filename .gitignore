# Ignore the API key file to prevent exposing sensitive credentials
api_key.json

# Ignore Python virtual environment directory to avoid committing dependencies
# and environment‑specific files to the repository
.venv/

